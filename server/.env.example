PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# Builder D fills these in
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Merge Agent Handler — YouTube-backed lounge jukebox (optional; falls back to
# the hardcoded playlist if unset). Use the *test* API key for is_test users.
MERGE_API_KEY=
MERGE_TOOL_PACK_ID=
MERGE_REGISTERED_USER_ID=
# MERGE_JUKEBOX_QUERY=2010s pop official music video
# MERGE_JUKEBOX_COUNT=10
