// Builder A — presence & room state (lounge, who's where, lock state).
// Not wired into server.js yet. Export functions here, then require()
// them from server.js when ready — don't edit server.js's terminal logic
// directly, ping the group first if you need to.

module.exports = {};
