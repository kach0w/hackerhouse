// Builder D — agent-done webhook (POST /notify from the Claude Code
// Stop hook). Not wired into server.js yet. Export an Express router
// here, then app.use() it from server.js when ready.

module.exports = {};
