{
  "name": "hackerhouse-local-agent",
  "version": "2.0.0",
  "private": true,
  "description": "Local companion that gives each visitor their own real terminal, on their own machine, with their own Claude Code login. Registers with the shared server's /terminal namespace so owners and spectators see the same output they always did.",
  "main": "agent.js",
  "scripts": {
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/*/spawn-helper 2>/dev/null || true",
    "start": "node agent.js"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "socket.io-client": "^4.8.1"
  }
}
